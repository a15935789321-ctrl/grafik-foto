# 2026-03-31

## Реализовано

- В генератор графика добавлено жёсткое правило: у сотрудника не может быть больше `3` обычных выходных подряд.
- Ограничение встроено прямо в решатель: варианты, создающие цепочку `OFF` длиной `4+`, сразу отбрасываются во время подбора.
- Добавлена финальная защитная проверка решения перед назначением дежурств, чтобы график с длинной серией выходных не считался допустимым.
- В пост-отчёт после генерации добавлена отдельная строка проверки по каждому сотруднику: соблюдён ли лимит на подряд идущие выходные.
- После ревизии структуры проекта удалён дублирующий `ready-to-paste/Code.gs`; единственным источником правды оставлен основной `Code.gs`.
- Для упрощения навигации по проекту инструкция по ручной кнопке перенесена в `README.md`, временная папка `ready-to-paste` очищена от рабочих файлов.
- Создан локальный Codex skill `google-sheets-photo-schedule` в `C:\Users\f.druzhinin\.codex\skills\google-sheets-photo-schedule` с кратким `SKILL.md`, UI-метаданными и reference-файлами по архитектуре, правилам и адаптации.

## Важные решения

- В лимит включаются только дни со статусом `OFF`.
- Отпуск (`VACATION`) не считается выходным для этого правила и разрывает серию.
- При невозможности уложиться в новое ограничение генератор завершает работу общей ошибкой о несовместимых правилах.
- Python/uv в окружении не найдены, поэтому штатный `quick_validate.py` не запускался; вместо него выполнена файловая и содержательная проверка структуры skill.
